apiVersion: v1
kind: Service
metadata:
  name: back-end-service
  labels:
    app: crud-go-react
    tier: backend
  namespace: crud-app
spec:
  selector:
    app: crud-go-react
    tier: backend
  namespace: crud-app
  ports:
  - port: 8080
    targetPort: 8080
