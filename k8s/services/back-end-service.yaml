apiVersion: v1
kind: Service
metadata:
  name: back-end-service
  namespace: crud-app
  labels:
    app: crud-go-react
    tier: backend
spec:
  selector:
    app: crud-go-react
    tier: backend
  ports:
  - port: 8080
    targetPort: 8080
  type: LoadBalancer  
